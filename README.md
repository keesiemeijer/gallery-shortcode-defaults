gallery-shortcode-defaults
==========================

Set new gallery defaults in the media uploader. This plugin sets the attachment link url to "Media File" instead of "Attachment Page". Other defaults like 'Columns', 'Random Order' and 'Size' can be set in your (child) theme's functions.php file.
